scenarios:

  high_recall:
    default:
      index_params: {algorithm: ivf, nlist: 256, niter: 5}
      query_params: {nprobe: 64}

  fast:
    default:
      index_params: {algorithm: ivf, nlist: 256, niter: 5}
      query_params: {nprobe: 16}

  memory:
    default:
      index_params: {algorithm: ivf, nlist: 256, niter: 5}
      query_params: {nprobe: 64}
